apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-backend
spec:
  ports:
  - port: {{ .Values.service.port }}
    targetPort: 5001
  selector:
    app: {{ .Release.Name }}-backend
  type: {{ .Values.service.type }}
